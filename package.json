{
  "name": "ui5.walkthrough",
  "version": "1.0.0",
  "description": "the UI5 walkthrough application",
  "scripts": {
    "start": "ui5 serve -o test/mockServer.html",
    "build": "ui5 build -a",
    "serve-dist": "ws --compress -d dist --open"
  },
  "devDependencies": {
    "@ui5/cli": "^3.8.0",
    "local-web-server": "^5.3.0",
    "ui5-middleware-simpleproxy": "^3.2.9"
  }
}
